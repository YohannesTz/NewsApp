<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yohannes.app.dev.newsapp">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_application_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/configuration"
        android:roundIcon="@mipmap/ic_application_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Articlemedium"
            android:label="@string/title_activity_articlemedium"
            android:theme="@style/ScrollingActivityTheme" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NoConnection"
            android:theme="@style/SplashScreenTheme" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="News App"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/SplashScreenTheme" />
        <activity android:name=".CreateActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/SplashScreenTheme" />
        <activity
            android:name=".Settings"
            android:label="@string/title_activity_settings" /> <!-- <activity android:name=".Settings"></activity> -->
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/ScrollingActivityTheme" />

        <activity android:name=".ChangeAvatar" />
        <activity android:name=".SavedArticles" />
        <activity
            android:name=".IntroActivity"
            android:label="IntroActivity"
            android:theme="@style/SplashScreenTheme" />
    </application>

</manifest>